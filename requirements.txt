# Core service. Enough to run the API, the interface and the demo pipeline.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7,<3.0
cryptography>=42.0

# Testing
pytest>=8.0
httpx>=0.27
