# Runtime. Enough to serve the API, the interface and the scripted pipeline.
#
# Kept lean on purpose: this file is what a serverless bundle installs, and
# every megabyte here is cold-start time and bundle-size risk on that host.
# Test-only packages live in requirements-dev.txt.
fastapi>=0.115,<1.0
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7,<3.0
cryptography>=42.0
